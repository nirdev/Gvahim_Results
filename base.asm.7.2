IDEAL
MODEL small
STACK 100h
DATASEG
; -------------------------- 
; Your variables here
; --------------------------
;var1 db -100
;var2 db 100
arr_a db 4 dup (?)
arr_b db 4 dup (?)
arr_c dw 4 dup (?)

CODESEG
start:
	mov ax, @data
	mov ds, ax
;       //Set initial values to array a.
	mov [arr_a + 0],9
	mov [arr_a + 1],8
	mov [arr_a + 2],7
	mov [arr_a + 3],6
;       //Set initial values to array b.
	mov [arr_b + 0],3
        mov [arr_b + 1],1
        mov [arr_b + 2],-1
        mov [arr_b + 3],-3 aa

;	//Save arr_a to bl and substract arr_b from it, then insert result from ax to arr_c
	mov bx,0
	mov bl, [arr_a + 0]
	sub bl,[arr_b + 0]
	mov [arr_c + 0], bx 

;	//Save arr_a to bl and substract arr_b from it, then insert result from ax to arr_c
	mov bx,0
	mov bl, [arr_a + 1]
	sub bl,[arr_b + 1]
	mov [arr_c + 1], bx 

;	//Save arr_a to bl and substract arr_b from it, then insert result from ax to arr_c
	mov bx,0
	mov bl, [arr_a + 2]
	sub bl,[arr_b + 2]
	mov [arr_c + 2], bx 

;	//Save arr_a to bl and substract arr_b from it, then insert result from ax to arr_c
	mov bx,0
	mov bl, [arr_a + 3]
	sub bl,[arr_b + 3]
	mov [arr_c + 3], bx 




;----------------------------------------------------
	
exit:
	mov ax, 4c00h
	int 21h
END start


